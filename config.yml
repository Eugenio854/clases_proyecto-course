title: Clases_Proyecto
description: Proyecto de Sensores
template:
  repo: clases_proyecto-template
  name: clases_proyecto
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
